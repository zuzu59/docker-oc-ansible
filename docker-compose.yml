# source: 
# 
# zf190903.1739

version: '3'

services:
    oc-ansible:
        build: .
        image: zubuntu
        container_name: oc-ansible
        restart: always
        volumes:
            - .:/myubuntu
            - /keybase:/keybase
        ports:
            - "52222:22"
        stdin_open: true
        tty: true


